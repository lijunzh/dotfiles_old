################################################################################
#
#														General Behavior
#
################################################################################

defshell -bash									# dash force a login shell
defscrollback 5000							# huge scrollback buffer
defutf8 on											# defaut UTF-8 ('screen -U')
bind s split										# split binding
defmousetrack on								# shift window focus by mouse click
termcapinfo xterm* ti@:te@			# mouse scroll
mousetrack on										# mouse swtich region focus
bind l layout next							# switch layout
shell "/bin/zsh"								# default shell


################################################################################
#
#																Appearnce
#
################################################################################

startup_message off             # no welcome message
chdir														# always start at home directory
altscreen on                    # alternative screen, reset after quit vim

# two-line status, with the current window highlighted
hardstatus alwayslastline
hardstatus string '%{= kG}[%{G}%H%? %1`%?%{g}][%= %{= kw}%-w%{+b yk} %n*%t%?(%u)%? %{-}%+w %=%{g}][%{B}%m/%d %{W}%C%A%{g}]'

# 256 colors
attrcolor b ".I"
termcapinfo xterm 'Co#256:AB=\E[48;5;%dm:AF=\E[38;5;%dm'
defbce on

################################################################################
#
#														Sessions and Layout
#
################################################################################

# screen -t default		0		zsh
# sessions
# screen -t local_main			0	bash
# screen -t local_top				1 bash
# screen -t local_bottom		2	bash
# screen -t local_upload		3 bash
# screen -t remote_monitor	4 bash
# screen -t remote_main			5 bash
#
# # layout
# layout autosave on
# layout new main
# select 0
# layout new local
# split
# select 1
# focus down
# select 2
# resize -v 15
# layout new remote
# select 3
# split
# focus down
# select 4
# focus up
# split -v
# focus right
# select 5
# layout select main

